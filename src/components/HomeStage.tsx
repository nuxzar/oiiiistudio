"use client";

import { WorkGallery } from "@/components/gallery/WorkGallery";

/**
 * Homepage IS the interactive field — same idea as Bettering.
 * No stacked marketing sections competing with the plane.
 */
export function HomeStage() {
  return <WorkGallery />;
}
